import { Component } from '@angular/core';

/**
 * ゲーム画面のルートコンポーネント
 * 既存のAppComponentの内容を表示
 */
@Component({
  selector: 'app-game-root',
  template: '<app-root></app-root>'
})
export class GameRootComponent {
}
